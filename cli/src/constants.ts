/** Number of leading characters shown when masking an API key for display.
 *  Matches the server-side `PREFIX_DISPLAY_LENGTH` so the CLI shows exactly
 *  the prefix the server records on the key. */
export const API_KEY_MASK_LENGTH = 11;
